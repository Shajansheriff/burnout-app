'use strict';

/**
 * @ngdoc function
 * @name burnoutApp.controller:ProjectsCtrl
 * @description
 * # ProjectsCtrl
 * Controller of the burnoutApp
 */
angular.module('burnoutApp')
  .controller('ProjectsCtrl', function () {
    this.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
